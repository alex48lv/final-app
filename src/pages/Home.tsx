import Categories from "../components/Categories/Categories";
import styled from "styled-components";

const Home: React.FC = () => {
    return (
        <Categories />
    );
};

export default Home;